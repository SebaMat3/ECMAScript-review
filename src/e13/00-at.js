const array = ["one", "two", "three", "four", "five", "six"]; 
// How can we access the last digit?
console.log(array[array.length - 1]);
// .at(-1) simpler way to say the same:
console.log(array.at(-1));